var featureExplorerController = (function () {

    function initialize(controllerSetup) {

    }

    function activate(rootDiv) {
        console.debug(rootDiv);
    }

    return {
        initialize: initialize,
        activate: activate
    };
})();
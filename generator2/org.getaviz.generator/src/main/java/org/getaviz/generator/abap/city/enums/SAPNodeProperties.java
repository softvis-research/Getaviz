package org.getaviz.generator.abap.city.enums;

public enum SAPNodeProperties {

    // alle Elemente
    element_id, object_name, type, type_name, creator, created, changed_by, changed, iteration,

    //spezifische Eigenschaften
    datatype, rowtype, length, decimals, domname, dataelement, structure, modifiers, local_class, container_id, uses_id

}

package org.getaviz.generator.java.enums;

public enum JavaNodeProperties {
    valid, sourceFileName, fileName, fqn, visibility, name, byteCodeVersion, md5, element_id, declares_id, type_name,
    effectiveLineCount, iteration
}
